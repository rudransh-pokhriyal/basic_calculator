import java.lang.*;
class inv_trigo.java
{
    double Asin(double deg)
    {
        return Math.toDegrees(Math.asin(deg));
    }

    
    double Acos(double deg)
    {
        return Math.toDegrees(Math.acos(deg));
    }


    double Atan(double deg)
    {
        return Math.toDegrees(Math.atan(deg));
    }
}